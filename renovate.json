{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": [
        "config:base",
        "group:all"
    ],
    "packageRules": [
        {
            "matchDepTypes": [
                "*"
            ],
            "commitMessagePrefix": "{{#if (equals depType 'dependencies')}}{{#unless (equals depType 'devDependencies')}}chore(deps):{{/unless}}{{else}}choredev(deps):{{/if}}"
        }
    ]
}
