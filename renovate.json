{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": [
        "config:base",
        "group:all"
    ],
    "packageRules": [
        {
            "matchDepTypes": [
                "*"
            ],
            "commitMessagePrefix": "{{#if (equals depType 'dependencies')}}chore(deps):{{else if (equals depType 'devDependencies')}}choredev(deps):{{else}}choredev(deps):{{/if}}"
        }
    ]
}
