{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": [
        "config:base",
        "group:all"
    ],
    "packageRules": [
        {
            "matchPackagePatterns": "*",
            "commitMessagePrefix": "{{#if (equals depType 'dependencies')}}chore(deps):{{else}}choredev(deps):{{/if}}"
        }
    ]
}
